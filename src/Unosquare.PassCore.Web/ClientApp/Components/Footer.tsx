import *  as React from 'react';
import Grid from '@material-ui/core/Grid/Grid';

const Footer: React.FunctionComponent<any> = () => {

    return (
        <React.Fragment>
            <Grid
                container={true}
                direction='row'
            >
                {/* for images */}
            </Grid>
            {/* here put the license text */}
        </React.Fragment>
    );
};